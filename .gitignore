/target
# Legacy: workspace-root copies that never actually existed.
/man
/autocomplete_shell_scripts
/.vscode
/sat-file
/cfr_semantic_files
/.opencode
/.claude
/docs
#Cargo.lock
*.code-workspace
*.log
.idea/
.DS_Store
Cargo.lock
/crates/*/config.toml
# Per-developer local copies of cli.toml / server.toml at the crate
# roots (used for local testing without the platform config dir).
# The real configs live under the OS config dir — see README.
/crates/*/cli.toml
/crates/*/server.toml
baseline.json
lcov.info
cli.toml.example
server.toml.example
/.cargo/config.toml
